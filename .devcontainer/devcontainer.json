{
  "name": "ERP Logístico",
  "image": "mcr.microsoft.com/devcontainers/php:1-8.3",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "ERP Logístico",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "editorconfig.editorconfig"
      ]
    }
  },
  "postCreateCommand": "docker-php-ext-enable pdo_sqlite sqlite3 2>/dev/null || true; composer install && (cp .env.example .env 2>/dev/null || true) && touch database/database.sqlite && php artisan key:generate && php artisan migrate --seed --force"
}
